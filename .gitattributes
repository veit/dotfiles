# SVG treated as an asset (binary) by default.
*.svg      text

# Normalizing line endings globally
# Disabled because of https://github.com/mathiasbynens/dotfiles/issues/149 :(
# * text=auto

# jq filter for Jupyter Notebooks
# Warning: If you want to do git rebase, you should first disable the following
# line.
*.ipynb filter=nbstrip_jq

